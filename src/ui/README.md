# `src/ui/`

Health bars, round timer, menus, and the training-mode overlays.

Dear ImGui lives here (ADR 0012). The hitbox viewer and frame-data inspector
are worth building early: they are the debugging environment for the sim, and
they double as a shipped feature.
